#include "pixelLUT.h"

// In this table: index is the inline number of the pixel (i.e. first pixel is index 0) value is the pixel in the image modulated with line length/3.
const int pixelLookupTable[TABLESIZE] {
	0, 0, 0, 0, 0
};
